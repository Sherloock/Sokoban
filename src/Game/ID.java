package Game;
public enum ID {
    // empty, wall, field, target 
    // doesnnt change
    Map(), 
    
    //box, box on target
    Box(), 
    
    Player(),
    
    //counters
    Counter(),
    Timer(),
    
    //solution
    Solution();
}
